const arr = [
    {name : '홍길동', age : 18},
    {name : '박길동', age : 28},
    {name: '김철수', age : 38},
];

// for(let i = 0; i < arr.length; i++) {
//     console.log(arr[i]);
// }

//forEach (반복문에서 사용) 반환값 (return) 이 없는
arr.forEach(item => {
    console.log(item);
});

//map 반환값이 있는 forEach

//filter (조건) 조건이 트루이면 반환값 반환



//reduce  줄여서 반환 